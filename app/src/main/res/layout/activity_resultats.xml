<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    tools:context=".view.ResultatsActivity">

    <!-- En-tête fixe -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appBarLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/white"
        app:elevation="0dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingHorizontal="13dp"
            android:paddingTop="32dp"
            android:paddingBottom="0dp">

            <!-- Ligne retour + titre + bouton filtres -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:layout_marginBottom="10dp">

                <ImageButton
                    android:id="@+id/btnRetour"
                    android:layout_width="28dp"
                    android:layout_height="28dp"
                    android:background="@drawable/bg_back_btn"
                    android:src="@drawable/ic_arrow_left"
                    android:padding="7dp"
                    android:contentDescription="Retour"
                    android:layout_marginEnd="8dp" />

                <TextView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Résultats"
                    android:textSize="14sp"
                    android:textStyle="bold"
                    android:textColor="@color/text_primary" />

                <Button
                    android:id="@+id/btnOuvrirFiltres"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Filtres"
                    android:textSize="11sp"
                    android:textColor="@color/text_primary"
                    android:backgroundTint="@color/white"
                    android:drawableStart="@drawable/ic_filter"
                    android:drawablePadding="4dp"
                    android:paddingHorizontal="11dp"
                    android:paddingVertical="5dp"
                    app:strokeColor="@color/border_secondary"
                    app:strokeWidth="1dp"
                    app:cornerRadius="9dp"
                    style="@style/Widget.MaterialComponents.Button.OutlinedButton" />

            </LinearLayout>

            <!-- Barre de recherche dans les résultats -->
            <com.google.android.material.textfield.TextInputLayout
                android:id="@+id/tilRecherche"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="8dp"
                app:boxBackgroundColor="#F5F5F5"
                app:boxCornerRadiusTopStart="9dp"
                app:boxCornerRadiusTopEnd="9dp"
                app:boxCornerRadiusBottomStart="9dp"
                app:boxCornerRadiusBottomEnd="9dp"
                app:boxStrokeWidth="0dp"
                app:boxStrokeWidthFocused="1dp"
                app:startIconDrawable="@drawable/ic_search"
                app:endIconMode="clear_text"
                style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox.Dense">

                <com.google.android.material.textfield.TextInputEditText
                    android:id="@+id/etRecherche"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:hint="Affiner les résultats…"
                    android:textSize="11sp"
                    android:imeOptions="actionSearch"
                    android:inputType="text"
                    android:paddingVertical="7dp" />

            </com.google.android.material.textfield.TextInputLayout>

            <!-- Chips des filtres actifs -->
            <com.google.android.material.chip.ChipGroup
                android:id="@+id/chipGroupFiltresActifs"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="8dp"
                app:singleLine="true"
                app:chipSpacingHorizontal="5dp" />

            <!-- Compteur de résultats -->
            <TextView
                android:id="@+id/tvNombreResultats"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="8dp"
                android:textSize="10sp"
                android:textColor="@color/text_secondary"
                tools:text="8 recettes correspondent à votre sélection" />

        </LinearLayout>

    </com.google.android.material.appbar.AppBarLayout>

    <!-- Contenu principal : grille de recettes -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvResultats"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingHorizontal="13dp"
        android:paddingTop="4dp"
        android:paddingBottom="16dp"
        android:clipToPadding="false"
        android:visibility="visible"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        tools:listitem="@layout/item_recette_card" />

    <!-- État vide -->
    <LinearLayout
        android:id="@+id/layoutVide"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="32dp"
        android:visibility="gone"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <ImageView
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:src="@drawable/ic_mood_empty"
            android:layout_marginBottom="10dp"
            android:contentDescription="Aucun résultat" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Aucune recette trouvée"
            android:textSize="13sp"
            android:textStyle="bold"
            android:textColor="@color/text_primary"
            android:layout_marginBottom="6dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Essayez d'élargir vos filtres ou de relancer le questionnaire avec d'autres ingrédients."
            android:textSize="11sp"
            android:textColor="@color/text_secondary"
            android:gravity="center"
            android:lineSpacingMultiplier="1.4"
            android:layout_marginBottom="16dp" />

        <Button
            android:id="@+id/btnModifierFiltres"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Modifier les filtres"
            android:textSize="11sp"
            android:layout_marginBottom="8dp"
            app:cornerRadius="9dp"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton" />

        <Button
            android:id="@+id/btnRefaireQuestionnaire"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Refaire le questionnaire"
            android:textSize="11sp"
            app:cornerRadius="9dp"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton" />

    </LinearLayout>

    <!-- Panneau de filtres latéral (DrawerLayout géré en code) -->
    <!-- Le panneau est un Fragment/BottomSheet instancié depuis l'Activity -->

</androidx.coordinatorlayout.widget.CoordinatorLayout>